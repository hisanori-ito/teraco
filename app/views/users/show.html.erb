<div class="container">
  <div class="row">
    <div class="col-lg-6 mt-5">
      <div>
        <div>
          <% if @user.image? %>
            <%= image_tag @user.image.url, class: "rounded-circle shadow-lg", width: '70%', height: 'auto' %>
          <% else %>
            <%= image_tag "kids-g837321511_640.png", class: "rounded-circle shadow-lg", width: '70%', height: 'auto' %>
          <% end %>
        </div>
        <div id="follow">
          <%= render 'relationships/follow', user:@user %>
        </div>
        <div class="d-flex justify-content-around mr-5 pr-5">
          <%= link_to "フォロー", user_follows_path(@user), class: "btn btn-outline-info"%>
          <%= link_to "フォロワー", user_followers_path(@user), class: "btn btn-outline-info" %>
        </div>
        <div class="d-flex justify-content-around mr-5 pr-5">
          <%= link_to "❤️いいねした記事", user_favorites_path(@user), class: "btn btn-outline-info" %>
          <% if @user == current_user %>
            <%= link_to "🔖ブックマークした記事", user_bookmarks_path(@user), class: "btn btn-outline-info" %>
          <% end %>
        </div>
      </div>
    </div>
    <div class="d-flex flex-column justify-content-between col-lg-6 pt-5 mt-5" >
      <div class="bg-light mb-3 h-25 rounded shadow-lg d-flex align-items-center justify-content-center">
        <%= @user.name %><br>
      </div>
      <div class="bg-light mb-5 h-50 rounded shadow-lg d-flex align-items-center justify-content-center">
        <% if @user.introduction? %>
          <%= @user.introduction %>
        <% else %>
          まだ紹介文はありません
        <% end %>
      </div>
      <div class="d-flex justify-content-between align-text-bottom h-25">
        <% if @user == current_user %>
          <div>
            <%= link_to "👕編集する", edit_user_path(@user), class: "btn btn-outline-info" %>
          </div>
          <div>
            <%= link_to "😢退会する", user_registration_path, method: :delete, data: { confirm: '本当に退会しますか？あなたの記事は全て消えてしまいます！' }, class: "btn btn-outline-danger" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
  <div class="row my-3">
    <div class="col-lg-12 text-center">
      <h2 class="font-weight-bold mt-3">投稿した記事</h2>
    </div>
  </div>
  <div class="row">
    <div class="col-lg-12">
      <div>
        <% if @posts.count == 0 %>
          該当する記事はありませんでした😔
        <% else %>
          <%= render "posts/index", { posts: @posts } %>
        <% end %>
      </div>
    </div>
  </div>
</div>
