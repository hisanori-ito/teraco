<h5>タグが<%=@tag.name%>の記事一覧</h5>

<% @tags.each do |tag|%>
  <%=link_to tag.name, search_tag_path(tag_id: tag.id) %>
  <%="(#{tag.posts.count})" %>
<% end %>

<% @posts.each do |post| %>
  <%= link_to post_path(post.id) do %>
    <%= post.title %>
  <% end %>
<% end %>