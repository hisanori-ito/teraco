<div class="container">
  <%= render "posts/search", { tags: @tags, word: @word } %>
  <div class="row">
    <div class="col-lg-12">
      <% if @posts.count == 0 %>
        該当する記事はありませんでした😔
      <% end %>
      <%= render "posts/index", { posts: @posts } %>
    </div>
  </div>
</div>