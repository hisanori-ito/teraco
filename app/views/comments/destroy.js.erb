<!--id="comments_areaにをrenderで指定した部分テンプレートに更新-->
$("#comment_area").html("<%= j(render 'comments/comment', post: @post ) %>");
$("#comment_form").html("<%= j(render 'comments/form', { post: @post, comment: @comment }) %>");
<!--フォームを送信したあとにフォームの値を空にします-->
$("textarea").val('');